<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bdqn.mall.dao.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.bdqn.mall.entity.User">
        <id column="id" property="id" />
        <result column="username" property="username" />
        <result column="password" property="password" />
        <result column="salt" property="salt" />
        <result column="realname" property="realname" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="level" property="level" />
        <result column="sex" property="sex" />
        <result column="creatime" property="creatime" />
        <result column="imageName" property="imageName" />
        <result column="integral" property="integral" />
        <result column="birthday" property="birthday" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, password, salt, realname, phone, email, level, sex, creatime, imageName, integral, birthday
    </sql>

</mapper>
